utils = {}

function utils.GiveCarKeys(vehicle, vehiclePlate)
    -- print(vehicle, vehiclePlate)
end

function utils.RemoveCarKeys(vehicle, vehiclePlate)
    -- print(vehicle, vehiclePlate)
end